// src/components/Header.js
import React from 'react';
import './Header.css'; // Optional: For custom styling

const Header = () => {
    return (
        <header className="header">
            <h1>SEE Grade Visualizer</h1>
        </header>
    );
};

export default Header;
